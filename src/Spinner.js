import "./Spinner.css";

function Spinner() {
  return (
    <div className="example">
        <span className="smooth spinner" />
        <span className="label"></span>
    </div>
  );
  }

export default Spinner;
